/**
 * Grid Manager Wrapper
 * Re-exports the GridManager from the parent directory
 */

// Import from parent directory (adjust path as needed)
import GridManager from '../../GridManager.js';

export default GridManager;
export { GridManager };